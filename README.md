# rsvp_flutter_app

A new Flutter project.

## Developer Setup

Set up the repository Git hooks after cloning:

```bash
bash tool/setup_git_hooks.sh
```

This enables the local `pre-commit` and `pre-push` hooks used by the project.
